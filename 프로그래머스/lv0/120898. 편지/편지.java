class Solution {
    public int solution(String message) {
        int answer = 0;
        answer = message.length() * 2;
        //.length는 배열의 길이 , length()는 문자열의 길이 구한다
        return answer;
    }
}